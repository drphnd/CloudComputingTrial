name: Laravel + Vite CI/CD

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Setup PHP (for Laravel)
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      # 3️⃣ Setup Node.js (for npm build)
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 4️⃣ Install dependencies
      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader

      - name: Install NPM dependencies
        run: npm ci

      # 5️⃣ Build frontend (Vite)
      - name: Build frontend assets
        run: npm run build

      # 6️⃣ Zip the project (exclude unnecessary files)
      - name: Package for deployment
        run: zip -r deploy.zip . -x "*.git*" "node_modules/*" ".env" "storage/*"

      # 7️⃣ Setup SSH to server
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.TEAM2_DRPHND_KEY }}

      # 8️⃣ Upload and deploy to Hestia
      - name: Upload and extract on server
        run: |
          scp -o StrictHostKeyChecking=no deploy.zip ${{ secrets.TEAM2_DRPHND }}@${{ secrets.TEAM2_DRPHND_HOST }}:/home/${{ secrets.TEAM2_DRPHND }}/deploy.zip
          ssh -o StrictHostKeyChecking=no ${{ secrets.TEAM2_DRPHND }}@${{ secrets.TEAM2_DRPHND_HOST }} "
            cd /home/${{ secrets.TEAM2_DRPHND }}/web/drphnd.team2lesgo.site/public_html &&
            unzip -o /home/${{ secrets.TEAM2_DRPHND }}/deploy.zip &&
            rm /home/${{ secrets.DEPLOY_USER }}/deploy.zip &&
            php artisan optimize
          "
